{
  "name": "item-manager-root",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "biuld": "npm run build --prefix frontend",
    "install": "npm install --prefix frontend"
  }
}
